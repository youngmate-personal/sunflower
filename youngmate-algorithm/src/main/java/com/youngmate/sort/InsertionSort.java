package com.youngmate.sort;

/**
 * @author ZhaoYanqi
 * @date 2021/2/3 0003
 */
public class InsertionSort {

    public static void insertionSort(int[] arr) {
        // 基本情况
        if (arr == null || arr.length < 2) {
            return;
        }
        // 插入过程：默认第一个元素时有序的，从第二个元素开始遍历，和前面的元素进行比较，当遍历

    }

}
